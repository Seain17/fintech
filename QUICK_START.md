# 🚀 빠른 시작 가이드

## 1분만에 앱 실행하기!

### 1단계: 의존성 설치 (최초 1회)

```bash
cd "C:\Users\cyunr\OneDrive\Desktop\바이브코딩"
npm install
```

설치 시간: 약 2-3분 소요

### 2단계: 개발 서버 실행

```bash
npm start
```

자동으로 브라우저가 열리고 `http://localhost:3000`에서 앱을 확인할 수 있습니다!

---

## 📱 주요 기능 테스트 가이드

### 로그인
1. 앱이 실행되면 스플래시 화면 (2.5초)
2. 온보딩 화면 → "처음이신가요? (시작하기)" 클릭
3. 소셜 로그인 버튼 아무거나 클릭

### 출금 신청 테스트 ⭐
1. 홈 화면 → "출금하기" 버튼 클릭
2. 은행 선택, 계좌번호, 예금주 입력
3. 출금액 입력:
   - **5만원 이하**: 그대로 진행
   - **5만원 초과**: 주민번호 입력 폼이 자동으로 나타남!
4. 제세공과금 자동 계산 확인 (22%)
5. "출금 신청하기" 클릭

### 포인트 내역 확인 ⭐
1. 하단 네비게이션 → "마이" 탭
2. "포인트 내역" 클릭
3. 상단 탭 (전체/적립/사용/소멸) 필터 테스트
4. 날짜 필터 (전체/1주일/1개월/3개월) 테스트

### 래플 응모 ⭐
1. 홈 화면 → 래플 배너 클릭 (또는 "혜택" 탭 → 래플 카드)
2. 상세 정보 확인 (응모 기간, 당첨자 발표일)
3. "100P로 응모하기" 클릭
4. 확인 팝업에서 "응모하기" 클릭
5. 포인트 차감 및 성공 메시지 확인

### 쇼핑 경유 ⭐
1. 하단 네비게이션 → "쇼핑 적립" 탭
2. 쿠팡/네이버/11번가 중 하나 클릭
3. "이동 중..." 화면과 유의사항 확인
4. 3초 카운트다운 후 쇼핑몰 오픈 (새 탭)

### 설정 및 알림 ⭐
1. 홈 화면 우측 상단 톱니바퀴 아이콘 (또는 마이 → 설정)
2. Push/SMS/Email 알림 토글 ON/OFF 테스트
3. "회원 탈퇴" 클릭 → 유의사항 팝업 확인

### 게임 플레이 ⭐
1. 홈 화면에서 게임 관련 메뉴 찾기 (또는 직접 URL 입력)
   - 룰렛: `http://localhost:3000/game/roulette`
   - 카드: `http://localhost:3000/game/card`
2. **룰렛 게임**:
   - "룰렛 돌리기" 버튼 클릭
   - 4초간 회전 애니메이션
   - 당첨 포인트 확인
3. **카드 게임**:
   - 카드 하나 선택
   - 플립 애니메이션과 함께 포인트 획득

---

## 🎯 전체 페이지 목록

### 메인 플로우
- `/` - 홈
- `/mission` - 미션
- `/shopping` - 쇼핑 적립
- `/benefits` - 혜택 (래플/오퍼월)
- `/mypage` - 마이페이지

### 상세 페이지
- `/withdraw` - 출금 신청
- `/point-history` - 포인트 내역
- `/raffle/1` - 래플 상세 (ID: 1)
- `/shopping/bridge/coupang` - 쇼핑 경유 (쿠팡)
- `/settings` - 설정
- `/game/roulette` - 룰렛 게임
- `/game/card` - 카드 게임

---

## 🐛 실행 중 오류 발생 시

### "npm을 찾을 수 없습니다"
→ Node.js 설치 필요: https://nodejs.org

### "포트 3000이 이미 사용중입니다"
```bash
# 다른 포트로 실행
PORT=3001 npm start
```

### 화면이 깨져 보임
→ 브라우저 개발자 도구(F12)에서 모바일 뷰 확인:
- 크롬: F12 → 모바일 아이콘 클릭
- 추천 기기: iPhone 12 Pro (390x844)

### CSS가 적용 안 됨
```bash
# 캐시 삭제 후 재시작
rm -rf node_modules/.cache
npm start
```

---

## 📊 현재 포인트: 26,350P

테스트용 Mock 데이터:
- 초기 포인트: 26,350P
- 출금 가능 금액: 2,635원 (10P = 1원)
- 이번 달 적립: +12,500P
- 이번 달 사용: -5,100P

---

## 🎨 커스터마이징 팁

### 색상 변경
`src/styles/global.css` 파일의 `:root` 섹션:
```css
--primary: #3182F6;  /* 메인 컬러 */
--accent: #FF6B35;   /* 강조 컬러 */
```

### 초기 포인트 변경
`src/App.js` 파일:
```javascript
const [userPoints, setUserPoints] = useState(26350); // 여기 수정
```

### 출금 최소 금액 변경
`src/screens/WithdrawDetailScreen.js`:
```javascript
else if (parseInt(formData.amount) < 5000) {  // 여기 수정
  newErrors.amount = '최소 출금 금액은 5,000원입니다';
}
```

---

## 📞 문제가 해결 안 되나요?

1. `node_modules` 폴더 삭제 후 재설치:
```bash
rm -rf node_modules
rm package-lock.json
npm install
```

2. 브라우저 캐시 완전 삭제 (Ctrl + Shift + Delete)

3. 다른 브라우저에서 테스트 (Chrome 권장)

---

## ✨ 완성된 기능 체크리스트

- [x] 스플래시 화면
- [x] 온보딩
- [x] 소셜 로그인
- [x] 홈 화면
- [x] 출금 신청 (은행, 계좌, 주민번호, 세금 계산)
- [x] 포인트 내역 (필터링)
- [x] 래플 상세 (응모 팝업)
- [x] 쇼핑 경유 (유의사항)
- [x] 설정 (알림 토글, 회원탈퇴)
- [x] 게임 (룰렛, 카드)
- [x] 하단 네비게이션
- [x] 토스트 알림

---

🎉 **모든 준비가 완료되었습니다!**

`npm start` 명령어 하나로 앱을 실행하고 모든 기능을 테스트해보세요!
